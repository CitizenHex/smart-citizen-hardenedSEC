PyQt6==6.10.0
lxml==6.1.1
pyinstaller==6.3.0
pyperclip==1.8.2

# Ed25519 verification for manually approved, signed portable updates.
# This nested file pins the exact Windows build-wheel hashes.
-r requirements-update-security.txt
